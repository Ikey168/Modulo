{
  "name": "modulo-android-shell",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "sync": "cap sync android",
    "open": "cap open android",
    "smoke:android": "node scripts/smoke-android.mjs",
    "build:debug": "npm --prefix ../.. ci && npm --prefix ../.. run build:strict --workspace=frontend && npm ci && npm run sync && cd android && ./gradlew assembleDebug --console=plain",
    "upgrade:android": "node scripts/upgrade-android.mjs",
    "build:release": "npm run sync && cd android && ./gradlew assembleRelease bundleRelease --console=plain"
  },
  "dependencies": {
    "@capacitor/android": "8.5.2",
    "@capacitor/app": "^8.1.1",
    "@capacitor/browser": "^8.0.4",
    "@capacitor/core": "8.5.2"
  },
  "devDependencies": {
    "@capacitor/cli": "8.5.2",
    "typescript": "^5.2.2"
  }
}
