# MikroTik artifact custody

RouterOS exports and binary backups are generated by the desktop timer and
stored in the encrypted Cryptomator vault. This directory intentionally does
not contain a configuration export or binary backup. See Knowledge Note #150.
