# OpenWrt artifact custody

Archer C6 `sysupgrade -b` archives and package manifests are generated by the
desktop timer and stored in the encrypted Cryptomator vault. This directory
intentionally does not contain an archive or wireless configuration contents.
See Knowledge Note #145.
