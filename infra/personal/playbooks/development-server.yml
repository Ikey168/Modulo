---
- name: Bootstrap the declared remote development server
  hosts: development_servers
  gather_facts: true
  become: true
  roles:
    - linux_baseline
    - backup_client
    - remote_development
    - private_network
    - container_host
