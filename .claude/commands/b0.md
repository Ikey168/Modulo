Show the current B0 (Core/Experience Boundary Refactor) milestone status in one glance.

Read the following files and print a consolidated status report:
1. The "Active milestone" table from `/home/Ikey/Modulo/CLAUDE.md`
2. The violation inventory from `/home/Ikey/Modulo/docs/architecture/B2-boundary-audit.md`
   — for each violation row show its current Status column (✅ Fixed / ⏳ Open)

Output format:

## B0 Milestone — issue status
| Issue | Title | Status |
(copy from CLAUDE.md active milestone table)

## Boundary violations
| # | Module | Status |
(one row per violation V1–V8, derived from the audit doc)

## Next up
State which open issue to work on next based on dependency order (the Depends on / Blocks / Parallel with fields in the GitHub issues).

Keep the output concise — no prose beyond the tables and the "Next up" line.
