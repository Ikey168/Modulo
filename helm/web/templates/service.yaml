apiVersion: v1
kind: Service
metadata:
  name: {{ include "modulo-web.fullname" . }}
  labels:
    {{- include "modulo-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "modulo-web.selectorLabels" . | nindent 4 }}
